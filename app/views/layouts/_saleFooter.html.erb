<div class="container">
  <% if action_name.to_sym == :show %>
<%= link_to 'Edit', edit_sale_path(@sale), class: "btn btn-primary" %> | <%= link_to 'Back', sales_path, class: "btn btn-primary" %>
  <% end %>
</div>
